import{useEffect,useRef,useState}from"react";import{FiSearch,FiX}from"react-icons/fi";import{useNavigate}from"react-router-dom";
const SearchDialog=({open,onClose})=>{const[query,setQuery]=useState(""),inputRef=useRef(null),navigate=useNavigate();useEffect(()=>{if(open)setTimeout(()=>inputRef.current?.focus(),0)},[open]);useEffect(()=>{if(!open)return;const close=e=>{if(e.key==="Escape")onClose()};document.addEventListener("keydown",close);return()=>document.removeEventListener("keydown",close)},[open,onClose]);const submit=e=>{e.preventDefault();const value=query.trim();if(!value)return;navigate(`/shop?search=${encodeURIComponent(value)}`);onClose()};if(!open)return null;return <div className="fixed inset-0 z-[80] flex items-start justify-center bg-neutral/55 px-4 pt-24 backdrop-blur-sm sm:pt-32" role="dialog" aria-modal="true" aria-labelledby="product-search-title" onMouseDown={e=>{if(e.target===e.currentTarget)onClose()}}><div className="w-full max-w-2xl rounded-[28px] bg-white p-5 shadow-2xl sm:p-8"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-[.25em] text-primary">Discover</p><h2 id="product-search-title" className="heading text-3xl">Search products</h2></div><button type="button" onClick={onClose} className="btn btn-circle btn-ghost" aria-label="Close search"><FiX size={22}/></button></div><form onSubmit={submit} className="mt-6 flex flex-col gap-3 sm:flex-row"><label className="relative flex-1"><span className="sr-only">Search products</span><FiSearch className="absolute left-4 top-1/2 -translate-y-1/2 text-neutral/45"/><input ref={inputRef} value={query} onChange={e=>setQuery(e.target.value)} className="input input-bordered h-14 w-full rounded-full pl-12" placeholder="Search products…" autoComplete="off"/></label><button disabled={!query.trim()} className="btn btn-primary h-14 rounded-full px-8"><FiSearch/>Search</button></form></div></div>};export default SearchDialog;
