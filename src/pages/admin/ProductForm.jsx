import { useEffect, useMemo, useState } from "react";
import { useForm } from "react-hook-form";
import { useNavigate, useParams } from "react-router-dom";
import toast from "react-hot-toast";
import { getCategories } from "../../api/categories";
import { createProduct, getAdminProducts, updateProduct } from "../../api/products";
import { parseImageUrls } from "../../services/imageService";
const defaults = { name: "", description: "", category: "", subcategory: "", brand: "", price: "", compareAtPrice: "", sku: "", stock: 0, lowStockThreshold: 5, images: "", specifications: "{}", featured: false, bestseller: false, isNew: false, active: true };
const blankVariant = () => ({ sku: "", price: "", compareAtPrice: "", stock: 0, active: true, attributes: [{ key: "", value: "" }] });
const ProductForm = () => { const { id } = useParams(); const navigate = useNavigate(); const [categories, setCategories] = useState([]); const [variants, setVariants] = useState([]); const { register, handleSubmit, reset, watch, formState: { isSubmitting } } = useForm({ defaultValues: defaults }); const selectedCategory = watch("category"); const subcategories = useMemo(() => categories.find((item) => item.name === selectedCategory)?.subcategories || [], [categories, selectedCategory]); useEffect(() => { Promise.all([getCategories(), id ? getAdminProducts() : null]).then(([categoryData, productData]) => { setCategories(categoryData.categories); if (id) { const product = productData.products.find((item) => item._id === id); reset({ ...product, images: product.images.join("\n"), specifications: JSON.stringify(product.specifications || {}, null, 2), compareAtPrice: product.compareAtPrice ?? "" }); setVariants((product.variants || []).map((variant) => ({ ...variant, compareAtPrice: variant.compareAtPrice ?? "", attributes: Object.entries(variant.attributes).map(([key, value]) => ({ key, value })) }))); } }); }, [id, reset]); const changeVariant = (index, field, value) => setVariants(variants.map((variant, itemIndex) => itemIndex === index ? { ...variant, [field]: value } : variant)); const submit = async (values) => { try { const payload = { ...values, price: Number(values.price), compareAtPrice: values.compareAtPrice === "" ? null : Number(values.compareAtPrice), stock: Number(values.stock), lowStockThreshold: Number(values.lowStockThreshold), images: parseImageUrls(values.images), specifications: JSON.parse(values.specifications || "{}"), variants: variants.map((variant) => ({ sku: variant.sku, price: Number(variant.price), compareAtPrice: variant.compareAtPrice === "" ? null : Number(variant.compareAtPrice), stock: Number(variant.stock), active: variant.active, attributes: Object.fromEntries(variant.attributes.filter((item) => item.key && item.value).map((item) => [item.key, item.value])) })) }; if (id) await updateProduct(id, payload); else await createProduct(payload); toast.success("Product saved"); navigate("/admin/products"); } catch (error) { toast.error(error.response?.data?.errors?.join(", ") || error.message); } }; const input = "input input-bordered w-full"; return <section><h1 className="heading text-5xl">{id ? "Edit" : "Add"} Product</h1><form onSubmit={handleSubmit(submit)} className="mt-8 space-y-6 rounded-3xl bg-white p-8"><div className="grid gap-4 md:grid-cols-2"><label>Name<input className={input} {...register("name", { required: true })} /></label><label>Brand<input className={input} {...register("brand")} /></label><label className="md:col-span-2">Description<textarea className="textarea textarea-bordered w-full" {...register("description", { required: true })} /></label><label>Category<select className="select select-bordered w-full" {...register("category", { required: true })}><option value="">Select</option>{categories.map((item) => <option key={item._id}>{item.name}</option>)}</select></label><label>Subcategory<select className="select select-bordered w-full" {...register("subcategory")}><option value="">None</option>{subcategories.map((item) => <option key={item.slug} value={item.name}>{item.name}</option>)}</select></label>{[["Price", "price"], ["Compare At Price", "compareAtPrice"], ["SKU", "sku"], ["Stock", "stock"], ["Low Stock Threshold", "lowStockThreshold"]].map(([label, name]) => <label key={name}>{label}<input type={name === "sku" ? "text" : "number"} className={input} {...register(name, { required: ["price", "sku", "stock"].includes(name) })} /></label>)}<label className="md:col-span-2">Image URLs<textarea className="textarea textarea-bordered w-full" {...register("images")} /></label><label className="md:col-span-2">Specifications (JSON object)<textarea className="textarea textarea-bordered w-full font-mono" {...register("specifications")} /></label></div><div><div className="flex justify-between"><h2 className="heading text-2xl">Variants</h2><button type="button" onClick={() => setVariants([...variants, blankVariant()])} className="btn btn-sm">Add Variant</button></div>{variants.map((variant, index) => <div key={index} className="mt-4 rounded-2xl bg-base-200 p-5"><div className="grid gap-3 md:grid-cols-4"><input value={variant.sku} onChange={(event) => changeVariant(index, "sku", event.target.value)} className={input} placeholder="SKU" /><input type="number" value={variant.price} onChange={(event) => changeVariant(index, "price", event.target.value)} className={input} placeholder="Price" /><input type="number" value={variant.compareAtPrice} onChange={(event) => changeVariant(index, "compareAtPrice", event.target.value)} className={input} placeholder="Compare price" /><input type="number" value={variant.stock} onChange={(event) => changeVariant(index, "stock", event.target.value)} className={input} placeholder="Stock" /></div>{variant.attributes.map((attribute, attrIndex) => <div key={attrIndex} className="mt-3 flex gap-2"><input value={attribute.key} onChange={(event) => changeVariant(index, "attributes", variant.attributes.map((item, i) => i === attrIndex ? { ...item, key: event.target.value } : item))} className={input} placeholder="Attribute (Shade)" /><input value={attribute.value} onChange={(event) => changeVariant(index, "attributes", variant.attributes.map((item, i) => i === attrIndex ? { ...item, value: event.target.value } : item))} className={input} placeholder="Value (Rose Nude)" /><button type="button" onClick={() => changeVariant(index, "attributes", variant.attributes.filter((_, i) => i !== attrIndex))} className="btn">Remove</button></div>)}<div className="mt-3 flex gap-3"><button type="button" onClick={() => changeVariant(index, "attributes", [...variant.attributes, { key: "", value: "" }])} className="btn btn-xs">Add Attribute</button><button type="button" onClick={() => setVariants(variants.filter((_, i) => i !== index))} className="btn btn-xs btn-error">Remove Variant</button><label><input type="checkbox" checked={variant.active} onChange={(event) => changeVariant(index, "active", event.target.checked)} /> Active</label></div></div>)}</div><div className="flex gap-5">{[["featured", "Featured"], ["bestseller", "Bestseller"], ["isNew", "New"], ["active", "Active"]].map(([name, label]) => <label key={name}><input type="checkbox" className="checkbox checkbox-primary" {...register(name)} /> {label}</label>)}</div><button disabled={isSubmitting} className="btn btn-primary rounded-full">Save Product</button></form></section>; };
export default ProductForm;
